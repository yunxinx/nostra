# Cargo build artifacts
/target/

# Cargo backup files from `cargo edit`
**/*.rs.bk

# Cargo.lock is intentionally checked in — this is a binary crate and
# contributors should build against the same locked dependency graph.
# (Delete this comment when adopting your own convention.)

# macOS
.DS_Store

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# Local overrides
.env
.env.local
